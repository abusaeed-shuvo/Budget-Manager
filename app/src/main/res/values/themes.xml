<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.BudgetManager" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">#000</item>
    </style>

    <style name="Theme.Notes.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.BudgetManager" parent="Base.Theme.BudgetManager" />

    <style name="TransactionAlertDialog" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:windowBackground">@drawable/rounded_corner</item>
        <item name="android:windowTitleStyle">@style/TextAppearance.Material3.TitleLarge</item>
        <item name="android:textColorPrimary">#000</item>
        <item name="android:textAlignment">center</item>
        <item name="android:colorAccent">#000</item>
        <item name="android:gravity">center</item>

    </style>


</resources>